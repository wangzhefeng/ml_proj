task: pca_reduction
artifact_root: artifacts
source:
  type: csv
  path: dataset/pca_reduction/train.csv
split:
  strategy: random
  valid_size: 0.2
  test_size: 0.2
model:
  backend: sklearn
  name: pca
  params: {}
feature_pipeline:
  - name: numeric_only
    params: {}
tune:
  enabled: true
  method: grid
  cv_folds: 4
  scoring: null
  param_grid:
    n_components: [2, 3, 4, 5]
